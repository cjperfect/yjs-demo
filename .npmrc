# pnpm 设置
auto-install-peers=true
strict-peer-dependencies=false
prefer-frozen-lockfile=true

# 让 workspace 内部包优先解析
link-workspace-packages=true

# 加速安装
network-concurrency=16

# 注意：node-linker 配置已迁移到 pnpm-workspace.yaml（pnpm 12+）
